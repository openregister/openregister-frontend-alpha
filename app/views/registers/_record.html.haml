%tr{class: "js-filter-item", "data-filter-terms" => record[:item].map { |key, value| value }.uniq }
  - @register_metadata[:field_definitions].each do |field|
    %td{class: field[:item]['field']}
      = record[:item][field[:item]['field']] || "<span class='unknown'>No data found</span>".html_safe
